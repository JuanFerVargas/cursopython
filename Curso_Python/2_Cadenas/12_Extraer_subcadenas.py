# Separar cadenas (Split)

datos = 'Hola Mundo'
lista = datos.split()  # Por default separa cada elemento por espacios en blanco
print(lista)

datos = 'Juan,38,Colombia'
lista = datos.split(',')
print(lista)
